도로명주소 >시도,시군구, [읍면], 도로명,건물번호본번[-건물번호부번] (리명)
지번>시도, 시군구, [읍면], [리명] [법정동명] 지번본번-[지번부번],([시군구건물용])]
  
select * from seoul
if(bjd !=null){}

	where case when 법정동 is not null then 읍면 =;
	sql+="when 읍면 is null then 읍면 도로명 like '%')";
	
	select * from seoul
	where 시군구 ='동대문구' and 법정동명 like '%이문동%' 
		and 지번본번 like '%77%' and 지번부번 like '%27%';
	select * from seoul 
	where 시군구 ='동대문구' 
		and 법정동명 like '%이문동%'
		and 지번본번 like '%77%' and 지번부번 = 27
	--띄어쓰기 반드시 신경쓴다, 문자열은 따옴표를 붙인다.
		select * from seoul 
		where 시군구 ='동대문구' 
		and 법정동명 like '%이문동%'
		and 지번본번 like '%77%' and 
		지번부번 like '%%'
	-- like '%%' 에서 모든 걸 검색할 수 있다.
	select * from seoul
	where 시군구 ='동대문구' 
	and 도로명 like '%휘경로17길%'
	and 건물번호본번 like '%7%' and 건물번호부번 like '%%'; 
	
	select * from seoul 
	where 시군구 ='동대문구' 
	and 도로명 like '%휘경로17길%'
	and 건물번호본번 like '%7%' 
	and 건물번호부번 like '%%'

			
			